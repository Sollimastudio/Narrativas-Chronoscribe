name: 🧠 AutoDiagnóstico Pro Autocorretivo do Narrativas-Chronoscribe

on:
  workflow_dispatch:        # ✅ habilita o botão manual Run workflow
  schedule:
    - cron: "0 10 * * *"    # executa diariamente às 10h UTC

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  diagnostico:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Clonar repositório
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.AUTO_DIAGNOSTICO_TOKEN }}

      - name: ⚙️ Instalar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 📦 Instalar dependências
        run: |
          echo "🔍 Verificando dependências..."
          if [ -f package.json ]; then
            npm install --legacy-peer-deps || echo "⚠️ Erro durante instalação de dependências"
          else
            echo "❌ Nenhum package.json encontrado!"
          fi

      - name: 🎨 Verificar Tailwind config
        run: |
          echo "🎨 Verificando Tailwind config..."
          if [ ! -f tailwind.config.js ] && [ -f tailwind.config.ts ]; then
            echo "⚡ Corrigindo extensão do Tailwind..."
            cp tailwind.config.ts tailwind.config.js
          fi

      - name: 🧠 Testar build
        run: |
          echo "🏗️ Testando build..."
          npm run build || echo "❌ Falha de build detectada"

      - name: 🩺 Gerar relatório de diagnóstico
        run: |
          echo "📊 Gerando relatório..."
          mkdir -p diagnostics
          echo "Relatório gerado em $(date)" > diagnostics/report-$(date +%F).log
          echo "Verificações do ambiente:" >> diagnostics/report-$(date +%F).log
          node -v >> diagnostics/report-$(date +%F).log
          npm -v >> diagnostics/report-$(date +%F).log
          echo "✅ Diagnóstico concluído." >> diagnostics/report-$(date +%F).log

      - name: 🛠️ Criar Pull Request automático (se houver correções)
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.AUTO_DIAGNOSTICO_TOKEN }}
          branch: "auto-fix/diagnostico-$(date +%s)"
          title: "🧠 Correções automáticas do AutoDiagnóstico Pro"
          body: |
            Este PR foi gerado automaticamente pelo agente AutoDiagnóstico Pro.
            Inclui:
            - Correções Tailwind
            - Ajustes de build
            - Relatório de integridade do sistema
          commit-message: "fix: correções automáticas do AutoDiagnóstico Pro"
          labels: "autofix, sistema"
          delete-branch: true

      - name: ✅ Conclusão
        run: echo "✅ Diagnóstico Pro executado com sucesso!"

